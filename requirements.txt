# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Pydantic v2 + settings
pydantic==2.10.6
pydantic-settings==2.7.1
email-validator==2.2.0

# Database — MongoDB (async)
motor==3.7.1
pymongo==4.16.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.20

# HTTP client
httpx==0.28.1

# Embeddings — local semantic matching. Commented out: it pulls in torch (~2GB), and the app
# auto-disables embeddings under ~1.2GB RAM anyway (EMBEDDING_PROVIDER=off), so on a 1GB box
# it's pure dead weight. Uncomment on a >=2GB instance to enable semantic matching.
# sentence-transformers==3.4.1
numpy==2.2.2

# Selfie analysis for profile verification (OpenCV Haar face detection). Commented out on the
# free tier: the wheel bloats the build and cv2 can OOM a 512MB worker. Without it, verification
# gracefully degrades to "pending review" (see app/services/face.py). Uncomment on a >=1GB
# instance to enable automatic selfie verification:
# opencv-python-headless==4.10.0.84

# Payments (optional — booking deposit). Safe to omit in dev.
stripe==11.4.0

# Gemini — optional venue text enrichment during seeding only
google-genai==1.7.0

# Admin image-uploader web app (admin/image_uploader.py)
# Pinned to 1.39 (Tornado-based) for compatibility with FastAPI's starlette pin.
streamlit==1.39.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.3
redis==7.2.0
